# Maximum-Clique-Detection-in-a-Graph-Using-MIPS-Assembly
This project implements a maximum clique detection algorithm using MIPS assembly language. It reads a graph represented as an adjacency matrix from an input file, identifies the largest clique (if it exists), and outputs the results to a file.
